<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Transfer Wizard Form View -->
    <record id="view_repair_transfer_wizard_form" model="ir.ui.view">
        <field name="name">repair.transfer.wizard.form</field>
        <field name="model">repair.transfer.wizard</field>
        <field name="arch" type="xml">
            <form string="Inter-Branch Transfer">
                <group>
                    <group string="Source">
                        <field name="from_branch_id"/>
                        <field name="from_warehouse_id"/>
                    </group>
                    <group string="Destination">
                        <field name="to_branch_id"/>
                        <field name="to_warehouse_id"/>
                    </group>
                </group>
                <group>
                    <group>
                        <field name="transfer_date"/>
                        <field name="reference"/>
                        <field name="requested_by" readonly="1"/>
                    </group>
                    <group>
                        <field name="total_lines"/>
                        <field name="state" invisible="1"/>
                    </group>
                </group>
                <separator string="Reason"/>
                <field name="reason" placeholder="Reason for transfer..."/>
                <separator string="Products to Transfer"/>
                <field name="line_ids">
                    <list editable="bottom">
                        <field name="product_id"/>
                        <field name="available_qty"/>
                        <field name="quantity"/>
                        <field name="unit_cost"/>
                        <field name="notes"/>
                    </list>
                </field>
                <footer>
                    <button name="action_confirm_transfer"
                            string="Confirm Transfer"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancel"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Transfer Wizard Action -->
    <record id="action_repair_transfer_wizard" model="ir.actions.act_window">
        <field name="name">Inter-Branch Transfer</field>
        <field name="res_model">repair.transfer.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
